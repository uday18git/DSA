#include<stdio.h>
void main()
{
    int a,b,c;
    printf("enter 2 no");
    scanf("%d%d",&a,&b);// to scan two numbers in one line
    // scanf("%d",&a); to scan one number in a line
    c=a*b;
    printf("\nmultiple=%d",c);
}
